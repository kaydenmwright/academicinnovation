{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pip install -r requirements.txt && npm install && make"
  },
  "deploy": {
    "startCommand": "python3 -m http.server $PORT"
  }
}
